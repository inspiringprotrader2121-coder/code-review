# Orvex Review — per-repo config (place in repo root)
mode: normal # strict | normal
include_docs: false

ignore:
  - "**/package-lock.json"
  - "**/pnpm-lock.yaml"
  - "**/dist/**"
  - "**/build/**"

max_comments: 8
max_tokens: 50000

run_semgrep: true

ignore_labels:
  - review-bot:ignore
